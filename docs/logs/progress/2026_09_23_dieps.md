# Tiến độ — 2026-09-23 — dieps

> **Tệp nhật ký theo người, append-only.** Mỗi phiên mở một tệp mới `YYYY_MM_DD_<tên>.md`.
> Không sửa tệp của người khác. Việc **cấp dự án** ghi ở `STATUS.md`, không ghi ở đây.

## Đã làm

- Dựng **Project Planning** và **Research Proposal** theo template học phần → `reports/md/`.
- **Slide báo cáo tiến độ** 23 slide, có section ứng dụng sáu màn hình → `reports/presen/01_progress_report/progress_slide.md`.
- Viết lại `notes/survey_overview.md` thành tổng quan **bốn vòng** khảo sát (trước chỉ mô tả Round 3).
- Chuyển `pillar_strength` từ HTML sang markdown và dời vào `notes/`.
- **Tái cấu trúc `refs/`:** mỗi cụm có `SOURCES.md` (dữ liệu, sửa tay) và `INDEX.md` (bản chụp); `refs/INDEX.md` thay bằng bản dựng từ 8 cụm, giữ Mục 9 bằng tay.
- `RULES.md` mục 5: thêm ngoại lệ chữ hoa cho file điều hướng và quản lý context (`INDEX.md`, `SOURCES.md`, ...).

## Bằng chứng

- Commit: `7d6dc26` · `0288acd` · `6ad082e` · `f4ff838`.
- Kiểm tra toàn vẹn `refs/`: **35 bản ghi khớp 35 PDF**, không có PDF mồ côi, 28 DOI đối chiếu được với catalog.
- Kiểm tra link toàn repo: 132 link nội bộ trong 34 tệp `.md`, 0 link hỏng.

## Còn lại cho người khác

- **Rà 14 bản ghi `evidence_basis: suy_tu_thiet_ke`** trong `refs/*/SOURCES.md` — đây là các mức bằng chứng do agent suy từ loại thiết kế, chưa có nguồn ghi sẵn.
- **Chốt mức bằng chứng của `Li, S. et al. (2024)`** — ma trận Round 3 ghi mức VI, còn bảng trụ cột tính như thể mức IV; chênh 2 điểm ở trụ cột P2 (`notes/pillar_strength.md` ghi 72, tính lại ra 70).

## Đang chặn

- Không có.
